// Mobile and tablet breakpoints in pixel values for media queries.
const BREAKPOINTS = {
  mobileMin: 320,
  mobileMax: 480,
  tabletMin: 768,
  tabletMax: 1024,
};

const TRANSITIONS = {
  duration: "120ms",
  timingFunction: "ease-out",
};

export { BREAKPOINTS, TRANSITIONS };
